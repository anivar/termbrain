# Demo Images

## demo.gif

A demo GIF will be added here showing:

1. Termbrain capturing commands automatically
2. Running `tb ai` to generate context
3. Using `tb search` to find commands
4. Viewing `tb stats` for analytics
5. Error learning in action

To create the demo:
```bash
# Use asciinema or terminalizer
asciinema rec demo.cast
# Convert to GIF using asciicast2gif
```